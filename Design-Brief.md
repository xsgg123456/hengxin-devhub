# 轻量化 IT 项目管理系统 · Design Brief

> 版本：v1.1
> 日期：2026-09-04
> 上游需求：[Product-Spec.md](./Product-Spec.md)
> 设计基线：Art Design Pro（Vue 3 + TypeScript + Element Plus）

## 0. 文档用途与优先级

本文档定义第一版产品及领导前端评审版的界面结构、视觉语言、核心页面、组件状态和交互规则，供后续设计稿与开发直接使用。领导前端评审版必须使用真实 Vue 页面和同一份可变 Mock 数据走通跨角色业务闭环，不能退化为静态效果图或互不关联的页面演示。

发生冲突时按以下顺序处理：

1. 已确认的设计稿；
2. 本 Design Brief；
3. Product Spec；
4. Art Design Pro 现有实现。

本文档不改变 Product Spec 的业务范围。没有在 Product Spec 中定义的审批、工单、聊天、AI、排名等能力，不得因为模板中存在对应页面而带入产品。

---

## 1. 设计目标

### 1.1 产品定位

这是一个面向公司内部正式 IT 项目的轻量化进度与风险工作台，不是完整的研发协作平台，也不是工程师的日常任务管理工具。

系统只解决三件事：

1. 业务人员用规范但简短的方式提交正式项目需求；
2. IT 工程师用一分钟以内维护项目大阶段、状态和关键日期；
3. 管理人员快速看清所有项目进度、风险和人员负载。

### 1.2 核心体验目标

- 管理人员打开首页后，30 秒内能找出延期、阻塞、长期未更新和即将到期的项目。
- IT 工程师在当前页面内完成一次进度更新，正常情况下不超过 1 分钟。
- 业务人员不需要理解研发管理术语，也能完成需求提交并查看本部门项目进度。
- 页面让人感觉专业、清楚、克制、可信，不追求炫技和“驾驶舱大屏感”。
- 领导无需开发者口头补充，即可连续完成“业务提交 → 管理立项 → 工程师更新 → 管理查看风险、负载和甘特”，并据此判断是否进入后端开发。

### 1.3 设计原则

#### 风险优先

管理首页首先展示需要处理的问题，再展示总体数字。装饰性图表不能抢占首屏。

#### 少填少点

默认值自动带出；只要求维护会影响管理判断的信息；高频动作使用侧边抽屉，避免来回跳页。

#### 信息真实

清楚区分原计划、当前预计、实际完成和风险事实。系统展示客观状态，不给工程师做绩效排名，不自动判断责任。

#### 结果可追

每个可见的核心业务操作都必须产生可观察的数据结果，并同步反映到相关角色页面。只弹成功提示但不改变数据、按钮可点却没有后续结果，都属于未完成。

#### 状态可读

颜色只用于增强识别，所有风险和状态必须同时显示文字、图标或具体天数，不能只靠颜色表达。

#### 模板做减法

继承 Art Design Pro 成熟的导航、卡片、表格、表单和权限框架；移除主题设置、多布局、多语言、多标签页、聊天、快捷入口和演示页面。

---

## 2. 目标用户与角色体验

系统只有三类权限角色。领导评审模式使用四个固定演示账号，分别覆盖业务人员、项目主负责人、项目协作人员和管理人员；主负责人和协作人员同属 IT 工程师角色，但操作权限不同。

| 角色 | 首要任务 | 默认首页 | 可见范围 | 设计重点 |
|---|---|---|---|---|
| 业务人员 | 提交正式需求、查看本部门项目进度 | 我的需求 | 本部门需求和项目 | 表单简单、术语易懂、结果明确 |
| IT 工程师 | 查看自己负责/参与的项目、维护进度 | 我的项目 | 自己作为主负责人或协作人的项目 | 更新入口明显、操作短、风险提示具体 |
| 管理人员 | 审批立项、查看全局进度、风险和负载 | 项目总览 | 全公司项目和人员数据 | 风险优先、数据紧凑、筛选高效 |

IT 负责人（PM）与公司管理层共用“管理人员”权限，不在界面中拆成第四种角色。

---

## 3. 视觉方向

### 3.1 关键词

专业、清楚、克制、可信、轻量、高效、数据密度适中。

### 3.2 正向参考

#### Art Design Pro：主要视觉和技术基线

参考内容：

- 左侧导航与顶部栏结构；
- 浅灰页面背景、白色内容卡片、蓝色主色；
- Element Plus 表格、表单、抽屉、弹窗、上传组件；
- 细边框、弱阴影和适中的圆角；
- Vue 3、TypeScript、Vite、Pinia、Element Plus 的工程组织方式。

使用方式：精简继承，不直接复制完整演示后台。

项目地址：[Art Design Pro](https://github.com/Daymychen/art-design-pro)

#### Ant Design：数据页面组织参考

参考“概览—筛选—列表—详情”的信息层级，以及企业后台中明确、可预测的交互方式。

参考地址：[Ant Design 数据可视化原则](https://ant.design/docs/spec/visualization-page-cn/)

#### Linear：紧凑列表和项目时间线参考

参考其低干扰的信息密度、快速筛选和项目列表阅读节奏，不照搬其深色主题或复杂研发概念。

参考地址：[Linear Features](https://linear.app/features)

### 3.3 明确不要的方向

- 不做大屏驾驶舱：不要深色背景、发光边框、巨大数字和地图装饰。
- 不做模板展示站：不要欢迎横幅、天气、日历、快捷入口、社交信息和无业务价值的图表。
- 不做重型研发平台：不要需求拆分、任务树、缺陷工单、代码仓库、工时、评论流和复杂审批流。
- 不使用大面积红色或橙色背景表示风险，避免页面长期处于“报警状态”。
- 不用百分比伪装精确进度；项目进度以七个阶段和简单状态表示。
- 不用人员排名、排行榜或红黑榜。
- 不用炫目的动效、渐变、玻璃拟态和 3D 插图。
- 不引入 AI 助手、AI 摘要或 AI 风险判断。

---

## 4. 信息架构

### 4.1 全局框架

- 左侧固定导航，展开宽度沿用约 230px，可手动折叠。
- 顶部栏保持单行，包含侧栏折叠、权限范围内的项目搜索、异常提醒入口和当前用户。仅在开发/评审构建中，当前用户区域同时承载轻量“演示模式”标识、演示身份切换、场景控制和数据重置入口。
- 异常提醒入口不建设独立消息中心：点击后直接进入当前角色有权限查看的风险项目筛选结果。
- 页面主体使用浅灰背景，内容放在白色卡片或白色表格容器中。
- 不启用多标签页；面包屑仅在项目详情、提交表单等二级页面显示。
- 主内容区在 1024、1280、1440px 桌面宽度下均可正常使用。

### 4.2 角色导航

#### 业务人员

1. 我的需求
2. 提交需求（作为主按钮或导航入口）

#### IT 工程师

1. 我的项目

#### 管理人员

1. 项目总览
2. 需求审批
3. 人员负载
4. 月度甘特图
5. 管理人员名单

归档项目不单独占用导航，在项目总览中通过“进行中 / 已完成 / 已取消”筛选查看。

### 4.3 页面清单

| 编号 | 页面 | 主要角色 | 对应需求 |
|---|---|---|---|
| SCREEN-001 | 登录与身份识别 | 全部 | REQ-001 |
| SCREEN-002 | 项目总览 / 风险工作台 | 管理人员 | REQ-006、REQ-007 |
| SCREEN-003 | 我的项目 | IT 工程师 | REQ-004、REQ-005 |
| SCREEN-004 | 我的需求 | 业务人员 | REQ-002、REQ-011 |
| SCREEN-005 | 提交 / 编辑需求 | 业务人员 | REQ-002 |
| SCREEN-006 | 需求审批 | 管理人员 | REQ-003 |
| SCREEN-007 | 项目详情 | 全部，按权限 | REQ-004、REQ-005、REQ-010、REQ-011 |
| SCREEN-008 | 人员负载 | 管理人员 | REQ-008 |
| SCREEN-009 | 月度甘特图 | 管理人员 | REQ-009 |
| SCREEN-010 | 管理人员名单 | 管理人员 | REQ-001 |
| SCREEN-011 | 不支持设备提示 | 全部 | REQ-001、AC-027 |

以上业务页面在领导评审模式下同时受 REQ-012 约束：共享同一份可变 Mock 数据，刷新保留，身份切换后按权限更新页面与操作，生产构建不保留任何演示入口。

---

## 5. 核心页面设计

### SCREEN-001 登录与身份识别

**目标**：让用户通过钉钉身份进入系统，不出现本地账号密码。

**桌面钉钉客户端**：自动发起免登。加载时显示居中的品牌占位、进度提示“正在确认你的身份…”。

**桌面浏览器**：显示钉钉扫码/授权入口，并说明登录后按组织身份自动分配权限。

**领导评审模式**：不展示虚假的钉钉登录页，直接使用固定演示账号进入系统。默认进入管理人员首页；顶部当前用户区域持续显示当前演示账号和轻量“演示模式”标识，可切换业务人员、项目主负责人、项目协作人员和管理人员。切换时若当前页面有未保存表单，先提示保存或放弃；没有未保存内容时直接进入所选身份的默认首页。

**成功**：根据角色跳转到对应首页。

**失败**：显示明确原因和“重新登录”按钮；没有组织权限时说明联系管理员，不暴露技术错误堆栈。

**空/加载/异常**：采用单卡片居中布局，不加载后台导航，避免未鉴权数据闪现。

### SCREEN-002 项目总览 / 风险工作台

**目标**：管理人员先看到风险，再进入项目。

**首屏结构**：

1. 页面标题“项目总览”，右侧显示数据更新时间；
2. 五个紧凑指标卡：进行中、已延期、长期未更新、已阻塞、待审批；
3. “今日需关注”紧凑表格；
4. 全部项目搜索与筛选表格。

**今日需关注排序**：已延期 → 已阻塞 → 长期未更新 → 即将到期。同一风险级别内按风险持续天数降序。

**风险表格建议列**：项目名称、需求部门、主负责人、当前阶段、项目状态、当前预计上线、风险、最近更新、操作。

**风险表达**：

- 行左侧使用 3px 红/橙色状态条；
- 风险列显示文字标签，如“阶段延期 3 天”“5 个工作日未更新”；
- 日期旁可显示具体差值；
- 不使用整行红底。

**筛选项**：关键词、需求部门、主负责人、当前阶段、项目状态、风险类型、时间范围、归档状态。

**交互**：点击项目名称或整行进入项目详情；点击指标卡直接套用对应筛选。

**状态**：

- 加载：指标卡与表格骨架屏；
- 空数据：显示“当前没有符合条件的项目”，保留清除筛选；
- 无风险：今日需关注区域显示绿色勾选和“当前没有需要关注的项目”；
- 错误：卡片内局部错误和重试，不让一个接口失败拖垮整页；
- 权限不足：跳转无权限页；
- 数据更新时间异常：顶部显示“数据可能不是最新，请刷新”。

### SCREEN-003 我的项目

**目标**：IT 工程师一眼知道自己要更新哪些项目，并快速完成更新。

**页面结构**：

1. 摘要条：我主责的项目、我协作的项目、需更新、风险项目；
2. 页签：我主责 / 我协作 / 已完成；
3. 项目列表或紧凑表格；
4. 主负责人项目行提供主要按钮“更新进度”。

**推荐列**：项目名称、我的角色、当前阶段、简单状态、当前阶段预计完成、当前预计上线、风险、最近更新、操作。

**协作人限制**：协作项目仍可提交自己的进展和阻塞说明，但不能修改项目总体阶段、状态和关键日期。界面需明确写“你是协作人”。

**状态**：

- 需更新项目优先置顶；
- 已阻塞和延期显示明确风险标签；
- 没有项目时显示“当前没有分配给你的项目”；
- 加载使用表格骨架；
- 更新成功后原行即时刷新并短暂显示成功反馈；
- 更新失败保留抽屉中的填写内容，允许重试。

### SCREEN-004 我的需求

**目标**：业务人员查看本部门提交记录及后续项目进度。

**页面结构**：

1. 标题“我的需求”，右上主按钮“提交需求”；
2. 状态筛选：全部、待审批、已退回、已立项、已完成、已取消；
3. 列表展示需求名称、提交人、提交时间、审批状态、项目阶段、主负责人、预计上线、操作。

**已退回**：在列表中直接显示一行退回原因摘要，提供“修改并重新提交”。

**已立项**：点击进入项目详情，只展示业务可见的信息，不显示管理操作。

**删除**：待审批和已退回需求允许提交人删除，操作前显示需求名称并二次确认；已立项后不显示删除入口。

**状态**：无数据、筛选为空、加载、接口错误、无权限、已撤回均需有明确文案。

### SCREEN-005 提交 / 编辑需求

**目标**：以一张简短表单收齐正式项目进入系统所需的信息。

**布局**：独立内容页，单栏表单，正文最大宽度约 820px；不使用抽屉，以保证 PRD 和原型附件上传清楚。

**字段顺序**：

1. 项目名称；
2. 需求部门（自动带出，只读）；
3. 提出人（自动带出，只读）；
4. 这次要解决什么问题（一句话）；
5. 期望上线日期；
6. PRD 文档（必填上传）；
7. HTML 原型（必填，可上传文件或填写链接）。

**页面底部**：固定或自然流式操作区，次按钮“取消”，主按钮“提交审批”。不增加草稿箱。

**校验**：字段旁就地提示，上传失败说明原因；只有全部必填项有效才允许提交。

**提交中**：按钮锁定并显示加载，防止重复提交。

**提交成功**：进入简洁结果页或返回“我的需求”，明确显示“已提交，等待管理人员审批”。

**编辑退回需求**：保留原内容，在顶部展示退回原因，提交按钮改为“重新提交”。

### SCREEN-006 需求审批

**目标**：管理人员快速完成单级立项审批和项目分配。

**主页面**：待审批表格，列出项目名称、需求部门、提出人、期望上线、提交时间、材料状态和操作。

**审批方式**：点击“审批”打开右侧抽屉。抽屉分为三段：

1. 需求摘要与材料链接；
2. 立项信息：优先级、主负责人、协作人、原计划上线日期；
3. 操作：退回、通过并立项。

**通过**：主负责人和原计划上线日期为必填；协作人可多选且不能与主负责人重复。

**退回**：点击退回后在抽屉内展开一行原因输入，原因必填，确认后完成退回。

**不增加**：多级审批、会签、上线审批、审批意见流、抄送选择。

**状态**：审批中防重复；成功后关闭抽屉并移除当前行；多人同时处理导致状态变化时提示“该需求已被处理”并刷新。

### SCREEN-007 项目详情

**目标**：在一页内看清项目处于哪里、是否有风险、计划怎么变化、谁在负责。

**顶部信息区**：

- 项目名称、项目状态、风险标签；
- 需求部门、提出人、主负责人、协作人；
- 原计划上线、当前预计上线、最近更新时间；
- 主负责人看到“更新进度”，管理人员看到符合权限的管理操作。

管理人员可在详情页纠正当前阶段和简单状态、调整当前计划、取消、手动归档或重新打开项目；只有从未产生进度记录的误建项目才显示“删除”。主负责人使用“更新进度”，管理人员纠正进度或调整计划时复用同一抽屉结构和日期变更规则，所有纠正均写入历史。

**七阶段进度条**：横向依次展示需求受理、立项评审、方案设计、开发编码、联调测试、上线部署、验收交付。

阶段状态规则：

- 已完成：实心主色圆点或勾选；
- 当前阶段：主色高亮并显示当前简单状态；
- 未开始：中性灰；
- 当前阶段延期：阶段下方显示红色“延期 X 天”；
- 不允许跳过阶段或自定义阶段。

**正文区域**：

1. 当前进展：最近更新内容、简单状态、当前阶段预计完成日；
2. 日期与风险：原计划、当前预计、差异原因、风险事实；
3. 更新记录：按时间倒序显示阶段、状态、更新人、说明；
4. 计划变更记录：原值、新值、原因分类、简短说明、操作人、时间；
5. 需求材料：PRD 与 HTML 原型链接/附件；
6. 项目基本信息。

**业务人员视图**：隐藏管理操作，但保留完整七阶段、项目状态、关键日期、负责人和可公开更新记录。

**结束状态**：验收交付完成后显示“已完成”并进入归档；已取消项目显示取消原因和时间，不提供更新入口。

### SCREEN-008 人员负载

**目标**：帮助管理人员看清每位工程师承担了多少正式项目，以及风险集中在哪里。

**结构**：顶部说明统计口径；下方紧凑表格，每位工程师一行。

**建议列**：工程师、主责项目数、协作项目数、当前阶段分布、时间重叠、延期、阻塞、长期未更新、操作。

**表达方式**：

- 不计算综合分数；
- 不排序为榜单，不显示名次；
- 默认可按风险数或主责项目数排序，但文案称“排序”而非“排名”；
- 点击人数/风险数字进入相应项目筛选结果。

**阶段分布**：使用紧凑堆叠条或标签数量，只展示实际数量，不做绩效好坏颜色。

**时间重叠**：显示同一时间窗口内预计上线日期重叠的主责项目数量，作为负载参考，不自动判定超负荷。

### SCREEN-009 月度甘特图

**目标**：查看指定月份中项目整体计划、当前预计上线和延期情况。

**结构**：

- 顶部月份切换、部门/负责人/风险筛选；
- 左侧冻结列：项目名称、主负责人、当前阶段；
- 右侧时间轴：按日或周刻度展示项目整体条；
- 今天使用细蓝色竖线标记。

**时间条**：从立项日期延伸到当前预计上线日期；原计划上线使用小刻度或空心标记，方便看出计划变化。

**风险表达**：延期项目的时间条末端或左侧行标记红色风险标签，不使用整条纯红色；已完成项目使用中性或成功色。

**交互**：悬停显示项目、日期、阶段和风险；点击整行进入项目详情。第一版不支持拖拽修改日期，不展示七阶段分段条。

**状态**：当月无项目、加载、局部错误、无权限和超出显示范围均有明确反馈。横向滚动时冻结左侧信息。

### SCREEN-010 管理人员名单

**目标**：维护高权限人员名单，支持身份规则中的管理权限覆盖。

**结构**：简单表格和“添加管理人员”按钮；字段为姓名、部门、钉钉身份标识、添加人、添加时间、状态、操作。

**添加**：使用小型弹窗搜索组织成员并确认。不能手工输入无法验证的人员标识。

**移除**：二次确认，提示权限影响；系统至少保留一名有效管理人员，避免全部移除。

**范围**：不建设通用角色权限编辑器，不允许创建自定义角色。

### SCREEN-011 不支持设备提示

**目标**：阻止第一版在手机、平板和过窄窗口中加载业务数据。

**内容**：居中展示桌面设备图标、标题“请在电脑端使用”和说明“第一版仅支持电脑浏览器或电脑钉钉客户端打开”。

**行为**：不加载项目、人员或需求数据；不显示侧栏和顶部栏；窗口恢复到支持宽度后可重新检测并进入登录流程。

---

## 6. 核心组件规范

### CMP-001 应用框架 App Shell

- 左侧导航展开约 230px，支持折叠为图标栏。
- 当前菜单使用浅蓝背景和蓝色文字，不使用重渐变。
- 顶部栏只保留必要动作。
- 所有纯图标按钮必须有可见 Tooltip 和 `aria-label`。

### CMP-002 指标卡

- 数字为主，标签和简短解释为辅。
- 点击态明显，可作为筛选快捷入口。
- 风险指标可使用红/橙数字，但白色卡片背景保持不变。
- 不放迷你折线图、环形图或无意义趋势动画。

### CMP-003 风险标记

统一支持：阶段延期、项目延期、长期未更新、已阻塞、即将到期、计划已调整。

每个标记必须包含文字；能计算天数时显示“X 天”。同一项目可显示多个标签，超过两项时显示前两项加“+N”。

### CMP-004 紧凑项目表格

- 1280px 高度下首屏尽量显示 8–12 行。
- 行高约 52–56px；表头固定时保持浅灰背景。
- 项目名称为主要链接；操作列固定在右侧。
- 风险项目使用左侧细色条，不使用整行底色。
- 支持键盘聚焦、排序、筛选、分页或轻量虚拟滚动。

### CMP-005 搜索与筛选条

- 默认显示最常用的关键词、负责人、风险和状态。
- 低频筛选收进“更多筛选”，避免一排十几个控件。
- 已启用筛选以可关闭标签展示，并提供“一键清除”。
- 搜索按 Enter 或短延迟触发，避免每个字符都闪烁重载。

### CMP-006 七阶段进度条

- 仅用于项目详情，不在所有列表重复铺开。
- 桌面宽度不足时允许横向滚动，不把七阶段挤成两行。
- 当前阶段需要同时用位置、文字和视觉强调表达。
- 阶段名称固定，不提供编辑或拖拽。

### CMP-007 更新进度抽屉

主负责人字段：当前阶段、简单状态、当前阶段预计完成日、当前预计上线日、进展说明、日期变更原因。

协作人字段：自己的进展说明、是否阻塞、阻塞说明。

- 抽屉默认宽约 480px；内容多时内部滚动，底部操作固定。
- 只有日期发生变化时才展开原因分类和一句话说明，并设为必填。
- 提交前显示变更摘要，不增加多步向导。
- 关闭有未提交内容时进行一次轻量确认。

### CMP-008 审批抽屉

- 默认宽约 560px。
- PRD 可按安全策略预览或下载；HTML/ZIP 原型只能作为附件下载，禁止在系统同源页面执行；材料失败时允许单项重试。
- “通过并立项”为主按钮；“退回”为次级危险动作，不与删除使用同一强度。
- 退回原因仅在选择退回时出现。

### CMP-009 文件与链接上传

- PRD 与 HTML 原型分开显示，避免用户传错。
- 显示文件名、大小、上传状态、重新上传和删除。
- 原型支持文件或链接二选一；链接需进行格式校验。
- 上传失败保留其他表单内容。

### CMP-010 人员负载表

- 所有数字均可追溯到项目清单。
- 不出现总分、排名、红黑榜和“低绩效”标签。
- 风险颜色只说明项目事实，不评价个人。

### CMP-011 简单甘特图

- 自定义为项目级时间轴组件，不照搬复杂甘特图库的编辑能力。
- 支持月份切换、今天线、冻结列、横向滚动、悬停提示和点击跳转。
- 第一版只读，不拖拽，不显示依赖关系，不做关键路径。

### CMP-012 反馈状态

统一提供：骨架加载、空状态、无权限、网络错误、保存中、成功、失败。

- 空状态采用线性图标加一行解释，不使用大型插画。
- 成功反馈使用轻提示，并同步刷新对应数据。
- 失败反馈必须告诉用户下一步，如“检查网络后重试”。

### CMP-013 确认弹窗

用于撤回、取消项目、重新打开、移除管理人员等影响状态的操作。

- 标题直接说明动作；
- 正文包含项目或人员名称；
- 危险按钮使用红色；
- 不用模糊的“确定 / 取消”作为唯一文案，改为“确认取消项目”等。

### CMP-014 演示身份菜单

- 仅在开发/评审构建中替换顶部当前用户菜单的扩展内容，不新增独立侧栏入口或业务页面。
- 顶部常驻展示当前演示用户姓名、角色和低强调度“演示模式”标签；标签使用信息色浅底，不能盖过业务风险提示。
- 下拉菜单依次列出业务人员、项目主负责人、项目协作人员和管理人员四个固定账号，同时显示部门或项目关系，当前账号使用勾选标记。
- 切换身份后关闭菜单、进入该身份默认首页，并同步更新导航、数据范围和操作控件；存在未保存内容时先用确认弹窗保护输入。
- 菜单底部以分隔线隔开“重置演示数据”。重置属于影响当前演示进度的操作，必须显示二次确认，并明确说明将恢复固定初始场景。
- 生产构建中不隐藏占位，而是完全移除演示标签、身份选项和重置入口，恢复普通当前用户菜单。

### CMP-015 演示场景控制

- 入口放在 CMP-014 演示身份菜单内，命名为“演示场景”，不占用顶部栏额外空间。
- 场景固定为：正常、空数据、加载、保存失败、无权限；不允许输入任意调试参数。
- 正常场景不显示额外状态；非正常场景在页面标题附近显示小型“场景模拟”标签，避免领导把模拟错误误认为产品故障。
- 加载场景可恢复到正常状态，不能永久阻塞；保存失败和无权限场景不得修改最近一次成功保存的数据。
- 场景切换只改变当前演示条件，不等同于重置业务数据；重置后场景自动回到“正常”。
- 生产构建完全移除场景控制和相关提示。

### 6.1 通用组件状态

所有可交互组件至少考虑以下状态：

| 状态 | 设计要求 |
|---|---|
| 默认 | 边界和用途清楚，不依赖占位符解释 |
| 悬停 | 轻微背景或边框变化，避免位移 |
| 聚焦 | 可见蓝色焦点环，键盘用户可识别 |
| 激活/选中 | 主色或浅蓝背景，并保留文字说明 |
| 禁用 | 降低对比度但仍可读，必要时说明原因 |
| 加载 | 局部骨架或按钮加载，防止重复提交 |
| 错误 | 就地红色提示并给出修复方向，不能只弹 Toast |

---

## 7. 视觉变量

### 7.1 色彩

沿用 Art Design Pro 的浅色基线，并固定为第一版唯一主题。

| 用途 | 色值 | 使用说明 |
|---|---|---|
| 主色 | `#5D87FF` | 主按钮、当前导航、链接、当前阶段 |
| 页面背景 | `#FAFBFC` | 全局内容背景 |
| 卡片背景 | `#FFFFFF` | 卡片、表格、抽屉 |
| 一级文字 | `#323251` | 标题、关键数据、正文重点 |
| 二级文字 | `#7987A1` | 说明、辅助字段 |
| 弱文字 | `#949EB7` | 时间、占位和次要信息 |
| 边框 | `#E6EAEB` | 卡片、输入框、表格分隔 |
| 浅分隔 | `#F2F4F5` | 表头、悬停和弱背景 |
| 危险/延期 | `#FF4D4F` | 延期、删除、严重错误 |
| 警告/临期 | `#FFAE1F` | 即将到期、计划变化提醒 |
| 成功 | `#13DEB9` | 已完成、保存成功 |
| 信息 | `#5D87FF` | 进行中、普通提示 |

色彩约束：

- 风险不能只用颜色表达，必须附文字或图标。
- 正文文字与背景达到 WCAG 2.2 AA 对比度目标。
- 成功色不用于“没有风险的人更优秀”等个人评价。

### 7.2 字体

优先使用系统字体，避免内网环境依赖外部字体资源：

```css
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Microsoft YaHei",
  "PingFang SC", Arial, sans-serif;
```

| 层级 | 字号/行高 | 字重 |
|---|---|---|
| 页面标题 | 22px / 30px | 600 |
| 区块标题 | 16px / 24px | 600 |
| 正文/表格 | 14px / 22px | 400/500 |
| 辅助文字 | 12px / 18px | 400 |
| 指标数字 | 26–30px / 34px | 600 |

不用超大标题，不使用全大写英文制造层级。

### 7.3 间距与密度

- 基础单位：4px。
- 常用间距：8、12、16、20、24、32px。
- 页面左右留白：1024px 时 16px；1280px 及以上 20–24px。
- 卡片内边距：16–20px。
- 表格行高：52–56px；密度保持“紧凑但不拥挤”。
- 表单字段垂直间距：20–24px。

### 7.4 圆角、边框与阴影

- 输入框/按钮：6–8px 圆角；
- 标签：6px 或胶囊形，仅用于短状态；
- 卡片：12–16px 圆角，保持 Art Design Pro 风格；
- 卡片默认使用细边框，不使用厚重阴影；
- 抽屉和弹窗可使用一层轻柔阴影，以区分层级。

### 7.5 图标

- 沿用项目已有 Iconify / Element Plus 线性图标体系。
- 同一语义保持同一图标，不混用填充、拟物和 Emoji。
- 图标不能代替文字表达关键业务状态。

### 7.6 动效

- 仅保留抽屉、弹窗、下拉和轻提示的标准过渡，建议 150–200ms。
- 不使用数字滚动、卡片漂浮、循环脉冲或首屏入场动画。
- 尊重系统“减少动态效果”设置。

---

## 8. 交互与文案规则

### 8.1 操作层级

- 每个页面最多一个视觉最强的主操作。
- 高频编辑尽量在当前页面右侧抽屉完成。
- 复杂材料提交使用独立页面，不塞进窄抽屉。
- 列表筛选改变查询条件，不改变业务数据，不需要确认。
- 删除、撤回、取消、移除权限等操作需要确认。

### 8.2 状态文案

项目简单状态固定为：

- 刚开始
- 进行中
- 接近完成
- 已完成
- 已阻塞

风险文案直接描述事实，例如：

- “当前阶段已延期 3 天”
- “项目预计上线晚于原计划 5 天”
- “已有 4 个工作日未更新”
- “当前已阻塞：等待测试环境”
- “距离阶段计划完成还有 1 天”

第一版计算口径固定为：当前阶段预计完成日是下一个工作日且阶段未完成时标记“即将到期”；进行中项目连续 3 个工作日没有进度更新时标记“长期未更新（停更）”。其余延期和计划变化规则以 Product Spec 的 REQ-005 为准。

避免使用：表现差、拖后腿、严重落后、低效人员等归责性词语。

### 8.3 日期表达

- 列表统一显示 `YYYY-MM-DD`。
- 详情和记录可显示 `YYYY-MM-DD HH:mm`。
- 原计划和当前预计必须并列展示，不能覆盖原计划。
- 日期变更记录必须保留旧值、新值、原因和时间。

### 8.4 通知入口

顶部铃铛只作为当前异常数量的快捷入口：

- 管理人员点击后进入“今日需关注”筛选；
- IT 工程师点击后进入“我的项目”中的需更新/风险筛选；
- 业务人员无异常任务时可隐藏铃铛，不建设独立站内信页面。

钉钉提醒仍按 Product Spec 的异常触发规则执行，界面不增加聊天式消息中心。

### 8.5 领导评审交互顺序

领导评审从“重置演示数据”开始，按固定顺序验证同一条业务记录：

1. 切换到业务人员，提交一条材料完整的需求；
2. 切换到管理人员，在需求审批中完成立项并指定主负责人和协作人员；
3. 切换到项目主负责人，在“我的项目”中找到新项目并更新阶段、状态、计划日期和进展；
4. 切换到项目协作人员，确认只能提交个人进展或阻塞，不能修改整体阶段和关键日期；
5. 切回管理人员，确认项目总览、风险、人员负载、月度甘特和项目详情均使用最新共同数据；
6. 刷新页面确认状态保留，再执行重置确认能恢复固定初始场景。

这条路径中的可见按钮必须有真实结果。尚未接后端的附件只模拟选择、校验、文件元数据和流程状态，界面必须显示“模拟上传”，不得伪装为已进入 MinIO。

---

## 9. 响应式与可访问性

### 9.1 设备范围

第一版只支持：

- 电脑浏览器；
- 电脑钉钉客户端内嵌页面。

重点验收宽度：1024px、1280px、1440px。手机钉钉、手机浏览器、平板和过窄窗口进入 SCREEN-011，不加载业务数据。

### 9.2 桌面适配

- 1024px 下侧栏可默认折叠，表格保留关键列，次要列允许隐藏或横向滚动。
- 1280px 为主要设计基准。
- 1440px 以上限制正文最大宽度或合理扩展表格，不把卡片无限拉宽。
- 抽屉不得遮住提交后的关键反馈。

### 9.3 可访问性

- 目标达到 WCAG 2.2 AA 的基本要求。
- 所有表单字段有持久标签，不用 Placeholder 代替 Label。
- 纯图标按钮提供可访问名称和 Tooltip。
- 键盘可完成导航、筛选、表单和抽屉提交。
- 焦点顺序与视觉顺序一致；弹窗关闭后焦点返回触发按钮。
- 错误信息与字段关联，屏幕阅读器可读取。
- 表格排序状态、页码和筛选结果数量应可感知。
- 风险、阶段和状态不能只依赖颜色。

---

## 10. 技术与实现约束

### 10.1 前端基线

后续产品代码以 Art Design Pro 的 Vue 3 技术体系为起点：

- Vue 3
- TypeScript
- Vite
- Element Plus
- Pinia
- ECharts（仅在确有必要的数据图表中使用）

开发采用“现有实现优先”原则：Art Design Pro 已有且符合需求的布局、路由、权限、请求封装、状态管理和通用组件直接复用或精简，不按效果图重新实现一套。只有项目风险表格、七阶段进度条、进度更新抽屉和只读甘特图等现有代码无法满足的业务部分才新增开发。

不得在当前 Harness 仓库的产品子目录中再次执行 `git init`。

### 10.2 模板精简清单

保留：

- 左侧导航和基础顶部栏；
- 路由、权限、状态管理和请求基础设施；
- 表格、表单、抽屉、弹窗、上传、分页；
- 通用图标和浅色设计变量。

删除或禁用：

- 主题设置面板；
- 深色模式；
- 多种布局切换；
- 多语言；
- 页面工作标签 / 多标签页；
- 聊天、消息中心演示、快捷入口；
- 非本产品需要的 Dashboard 和组件演示页面。

### 10.3 甘特图实现边界

Art Design Pro 当前没有可直接复用的项目甘特组件。第一版只需要只读的项目级时间轴，开发计划阶段应在以下方式中择一：

- 基于 DOM/CSS Grid 自建轻量时间轴；
- 选择许可证和体积合适的只读甘特组件。

无论采用哪种方式，都不能带入任务依赖、拖拽排期、关键路径等超出 Product Spec 的能力。

### 10.4 外部基线的处理

- Art Design Pro 采用 MIT License，复制或派生代码时保留必要的版权和许可证文本。
- `D:\Work_Project\art-design-pro` 作为代码母版和参考源；允许复制并改造其中的现有代码，但不直接在原模板仓库中开发本产品。
- 后续开发应在 Harness 根目录下创建独立产品子目录，并避免复制参考项目的 `.git`、`node_modules`、构建缓存和演示数据。

### 10.5 领导评审模式边界

- 页面只通过统一 service/repository 接口访问数据，不能在组件内直接导入互不关联的静态 JSON。
- 四个演示账号共享同一份浏览器本地数据；成功操作立即持久化，切换身份和刷新页面后继续使用同一条需求、项目和进度记录。
- 固定初始场景必须可重复恢复，重置前二次确认；浏览器本地数据损坏时不加载半成品页面，改为显示异常说明和重置入口。
- 页面、路由、表单、校验和状态管理采用最终生产结构。后端开发只替换 repository 实现，不重写页面交互。
- 原型与生产使用单向构建边界：生产构建无法打开身份切换、场景控制、数据重置或 Mock 回退。
- 领导没有明确确认流程、信息结构和核心交互前，不进入后端开发；评审意见先同步 Product Spec、Design Brief 和 DEV-PLAN，再修改前端。

---

## 11. 设计验收清单

设计稿或实现进入下一阶段前，逐项检查：

- [ ] 管理人员首页首屏先展示五个指标和“今日需关注”，没有装饰性大图表。
- [ ] 风险列表在常见桌面高度下可见约 8–12 行。
- [ ] 风险同时使用文字和颜色，整行没有大面积红/橙底色。
- [ ] 项目详情完整展示固定七阶段，顺序与 Product Spec 一致。
- [ ] 主负责人能从项目列表或详情直接打开更新进度抽屉。
- [ ] 协作人不能修改整体阶段和项目关键日期。
- [ ] 审批只有通过立项和退回，不出现上线审批或多级审批。
- [ ] 业务提交页只包含 Product Spec 要求的字段。
- [ ] 人员负载没有排名、总分或绩效评价。
- [ ] 甘特图是一项目一行的整体时间条，不显示阶段分段和拖拽能力。
- [ ] 已完成、取消和归档状态的入口与文案清楚。
- [ ] 页面没有 AI、聊天、工单、工时、任务拆分和小需求管理入口。
- [ ] 顶部栏没有主题、布局、多语言、全屏演示等模板控制项。
- [ ] 1024、1280、1440px 下布局可用。
- [ ] 手机、平板和过窄窗口只显示电脑端提示且不加载业务数据。
- [ ] 所有图标按钮有 Tooltip 与可访问名称。
- [ ] 加载、空数据、无权限、网络错误、提交中、成功和失败状态均有设计。
- [ ] 顶部当前用户区域可在评审模式切换四个固定演示账号，且清楚但克制地标识“演示模式”。
- [ ] 业务人员提交、管理人员立项、主负责人更新后，同一条记录在其他角色页面产生连续、可观察的数据变化。
- [ ] 项目协作人员没有整体阶段和关键日期编辑能力，直接越权访问也不会改变演示数据。
- [ ] 刷新页面保留最近一次成功状态；身份菜单内的一键重置经二次确认后恢复固定初始场景。
- [ ] 正常、空数据、加载、保存失败和无权限场景可稳定重现，失败场景不破坏最近一次成功数据。
- [ ] 所有可见核心业务按钮均有实际结果，不存在仅弹 Toast、死按钮或互不关联的孤立页面。
- [ ] 生产构建完全移除演示身份、场景控制、重置入口和 Mock 回退。
- [ ] 领导按固定评审路径走完跨角色闭环并明确确认后，才允许进入后端开发。

---

## 12. 暂不阻塞设计的后续替换项

以下内容不影响页面结构和开发计划，可在上线前替换：

- 正式系统名称；
- 公司 Logo 与浏览器图标；
- 公司品牌色是否最终替换 `#5D87FF`；
- 钉钉应用的正式名称与图标。

在这些素材提供前，统一使用纯文字系统名“IT 项目管理台”和 Art Design Pro 的蓝色主色，不生成临时品牌图形。

---

## A. AI / Agent 界面

不适用。第一版明确不接入 AI 能力，也不设计对话、生成、推理过程或 Agent 状态界面。
