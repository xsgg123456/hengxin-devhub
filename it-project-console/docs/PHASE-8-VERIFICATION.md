# Phase 8 管理看板与全量联调验收

日期：2026-09-08。范围按 DEV-PLAN Phase 8，钉钉身份与实际投递仍属 Phase 9，生产部署属 Phase 10。

## 需求与原位交付

| 交付 | 实现与复用 | 验证依据 |
|---|---|---|
| AC-017 风险优先与五指标 | dashboard-service 同源查询；原 project-overview 指标、分布图、项目卡片和详情；今日待办消费服务端风险持续天数 | 指标筛选、延期项目详情、服务端风险顺序 |
| AC-018 全项目筛选分页 | 原 use-project-overview-filters 保留账号/页面筛选记忆，真实查询输出全筛选图表明细及20条分页卡片 | 协作人筛选21条、第一页20/第二页1、统计与项目id集合一致 |
| AC-019/020 人员负载 | 原 person-workload 图表及项目弹出明细，服务端按月统计主责/协作、同期主责和风险 | 主责2/协作1以及多人唯一主责集成，真实页面主责21/协作0与主责0/协作21；逾期未交付仍占用当月 |
| AC-021/022 甘特 | 原 monthly-gantt 的230px冻结列、26px日刻度、双条、今日线、原计划交付点和详情 | 服务端跨月裁剪保留完整计划百分比；真实页面切月、风险文字、三档视口横向滚动冻结列 |
| 需求图表同源 | 原 my-demands 的提出人/部门环图和月趋势及表格；脚本抽到 use-demand-page | 服务端部门/提出人/状态/关键词筛选同源；无提交时间草稿不入趋势、空月份补零 |
| 管理名单 | 原 manager-grants 表格、组织候选弹窗和移除确认；manager-grant-service 事务锁、重新鉴权、幂等、审计 | 最后一人保护、并发撤权、旧新角色审计、同会话权限即时生效；UI添加→自撤权回首页 |
| 生产查询状态 | use-live-query 取消过期请求、180ms防抖、局部骨架/错误/重试，无mock回退 | 单测响应乱序/失败恢复/卸载/原型边界；真实网络失败恢复；原型全量回归 |

## 审查修正

- 阻塞排序从整体/管理纠正历史连续阻塞段起点计算上海日差；个人记录不重置，不解析“等待3号服务器”中的数字。最新重开事件切断旧阻塞段，防止取消重开后再次阻塞接续旧时长。今日待办使用服务端 attentionDays。
- 加载和失败时不同时渲染空图表。详情材料区上一阶段遗留的多余“>”已清理。
- 新聚合接口暴露原全IP 120/min容量不足；默认可配置600/min（1～10000整数），登录独立10/min，真实429保留retry-after与用户重试提示。测试遵守登录限流窗口。
- 浏览器测试准确定位Element选择器外层和当前aria-controls选项；通过API切身份后重新加载页面，避免同hash导航保留旧store。PRD校验定位字段错误节点，避免匹配到标签。不弱化业务断言。
- 领导评审测试等待详情关闭及路由清理后，从真实菜单进入甘特，避免直接hash跳转与详情关闭的路由替换竞争；63%进度、阻塞与持久化断言全部保留。

## 验证记录

- `pnpm check`：前端115项、API37项单测，前后端类型检查及生产构建通过；Web构建21.30s，证据 `output/phase8-check.log`。
- `pnpm --filter @it-project-console/api test:integration`：8文件53项通过，包含真实限流和并发名单；日志 `output/phase8-integration.log`。独立schema/Bucket已回收。
- `pnpm --filter @it-project-console/api test:browser`：4条完整真实流程通过（1.3m），包含旧需求/进度闭环和新增看板/名单；最终日志 `output/phase8-live-e2e.log`。21次本地创建样本最长83ms，甘特查询50ms；这是本地样本，不替代公司环境性能验收。
- `pnpm --filter @it-project-console/web test:e2e --workers=2`：34条通过（1.7m），包含三档母版布局与生产静态无mock；最终日志 `output/phase8-prototype-e2e.log`。
- 独立审查：Stage 1 PASS、Stage 2 PASS，无剩余HIGH/MEDIUM。最终复审覆盖重开阻塞段及领导流程导航时序修复，并读取全部最终日志与母版对照截图。

截图存于output的phase8-overview、phase8-project-cards和phase8-gantt系列。沿原母版布局，没有新应用壳；所有浏览器写入使用隔离测试数据库，用户4318/4322预览服务保留。本期没有数据库结构迁移，也没有push、部署或发送外部消息。

