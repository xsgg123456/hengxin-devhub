# Phase 9 验证记录

日期：2026-09-09。范围：复用旧项目钉钉登录、组织同步和工作通知，适配当前前后端。企业真实验收尚未执行。

## 已完成的代码验证

- 独立 code-reviewer：Stage 1 技术实现 PASS、Stage 2 PASS，最终未发现 HIGH/MEDIUM；已对照原 Art 源码、稳定登录图与既有总览图，企业真实验收独立保留待办。
- `pnpm check`：前后端类型检查通过，Web 128 项、API 52 项单测通过，前后端构建通过。
- `pnpm --filter @it-project-console/api test:integration`：12 个文件、67 项通过，包含最新企业管理员与样例账号共存回归；独立 schema 与 Bucket 已回收。
- Prisma schema 校验通过；新增通知日志、企业 unionId、OAuth challenge 三个增量迁移已应用本地数据库，未重置业务数据。
- 真实后端浏览器：7 项通过（最终轮 1.4 分钟），覆盖业务闭环、名单保护、新登录、扫码深链和手机阻断，隔离资源已回收。
- 原型与生产产物边界：34 项通过（1.9 分钟）；随后登录主题与动画调整已通过最终构建及真实后端浏览器验证。稳定截图为 `output/phase9-login.png`。
- Harness 自动化：19 项通过；本地 API `/health/ready` 返回 200，钉钉配置接口正确返回未配置。

本地原始日志保存在 `output/phase9-check.log`、`output/phase9-integration.log`、`output/phase9-live-e2e.log`、`output/phase9-prototype-e2e.log`，产物按仓库规则忽略，不提交。

## 关键回归

| 边界 | 验证内容 |
|---|---|
| 授权与身份 | 一次性 state、Cookie 绑定、过期拒绝、OAuth unionId 经企业接口映射、停用/外部身份拒绝、H5 后端会话 |
| 企业管理员 | 保留 seed 仍可初始化真实首位管理员；样例不进企业候选、不允许授予；最后真实管理员不能撤销，允许清理旧样例授权 |
| 组织 | 部门遍历、分页、完整同步事务、身份冲突拒绝、失效用户撤权与撤会话 |
| 风险 | 天数增长更新快照但不重复通知；上海工作日定时汇总，多项目合并、并发幂等、跨日变化、长中文长度预算 |
| 投递 | 样例过滤、持久认领、明确失败退避、受理与送达分离、未知结果不盲目重发 |
| 前端 | 原 Art 登录组件、配置失败重试、扫码站内深链、授权失败提示、手机阻断、需求详情深链 |

审查中修复了样例授权阻挡首位管理员、缺少管理风险汇总、样例被计入最后企业管理员三处问题；登录原组件增加固定主题选项并取消首屏入场动画，保留母版布局和装饰最终形态。原型测试同步新的登录文案和配置接口，并区分 Art SVG 资源请求与业务 API；冷启动等待实际页面就绪，业务操作性能断言保持原标准。

提交门禁重复验证暴露了测试套件间的队列干扰：其他用例的遗留任务占用了五条投递批次。汇总套件现在仅在隔离 schema 内暂缓这些旧任务，并在结束后恢复原时间；业务投递逻辑及断言保持不变。修复后全量 67 项接口集成再次通过。

## 企业环境仍待验收

需要企业应用凭据、回调域名/开放平台配置和指定测试收件人后，验证电脑钉钉免登、真实扫码、应用可见范围与组织权限、消息送达及深链。当前测试使用模拟远端响应与隔离数据库，不能替代真实企业联调。

实际通知默认关闭，未发送外部消息；旧项目及其生产环境未修改，当前样例业务记录保留。配置与复用映射见 [DINGTALK-INTEGRATION.md](DINGTALK-INTEGRATION.md)。
