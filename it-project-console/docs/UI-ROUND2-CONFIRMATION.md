# 第二轮界面优化确认记录

2026-09-11：用户查看基于现有前端生成的四张预览后确认“ok 先把这个定下来”，并追加要求参考 Downloads/AI IT项目管理台.html 中的甘特图。当前仍处于方案确认阶段，未授权将本轮预览直接上线。

## 已确认的界面基线

- 日期区间筛选增加今日、昨日、最近7/14/30天、本月、上月、本年、去年快捷选项。
- 项目展示编号采用 XM-2026-0001 形式，保留内部ID和关联关系。
- 项目卡片七环节改为大色块，区分灰色未开始、蓝色进行中、绿色按时完成、红色超期未完成及延期完成；用文字说明延期。
- 协作人员名单包含周熹；钉钉原部门保持不变，不恢复此前撤销的管理权限。真实人员身份及持久化配置尚待实施核实。
- 甘特悬浮提示紧邻鼠标，边缘避让。甘特主体新增要求见下方，尚未确认。

预览基线位于根目录 output/playwright/ui-round2/：01-project-blocks.png、02-date-shortcuts.png、03-gantt-tooltip.png、04-collaborator.png。预览使用本地示例数据，未改变正式代码和业务记录。

## 新增甘特图要求待对齐

已读取用户提供的HTML原文：renderGantt 使用上灰下彩双条，彩条长度由 overallProgress 百分比决定；状态决定蓝/橙/红/绿；左侧显示负责人及协作人员；周末底色和今日虚线；悬浮卡显示阶段、计划、人员、最近更新，跟随鼠标并避开边缘。

冲突：该HTML的百分比条与已上线的“取消人工百分比”要求不一致，不直接恢复。拟提案：保留灰色整体计划条，下方按工程师实际填写的阶段计划区间分段，颜色表达阶段状态，不称为实际耗时或百分比。缺排期不均分、不伪造；需求受理和立项评审无工程师计划日期时，仅在悬浮详情显示真实记录。

用户随后确认双排方案及05-gantt-stage-plans.png，最后明确“开始吧”。以下条款为本轮正式开发依据，覆盖上文待确认状态；本轮只开发与本地验收，部署另需明确要求。

## 正式验收条款

1. 所有实际业务日期范围筛选统一九个快捷项，最近N天含当天，自然月/年含完整首尾，上海日期口径；保留手选、清空及页面原筛选联动。工程师排期日期输入不加快捷筛选。
2. 对外项目编号XM-YYYY-NNNN，按立项上海年份分组顺序分配，超过四位自然增长；新项目数据库事务安全生成，存量一次性按createdAt/id稳定补齐；唯一且不复用、不随编辑改变。内部id保持不变，展示和搜索统一使用编号。
3. 周熹基于核实后的唯一钉钉身份配置持久工程师例外资格，保留部门；登录、通讯录同步、管理撤权、候选名单、分派及协作权限保持一致，不能用姓名硬编码授权，不能恢复其管理权限。支持可审计撤销例外资格。
4. 卡片原细线改为七个有名称/状态的大色块，以01预览为准；灰未开始或待排期、蓝进行中、绿按时完成、红延期未完成或延期完成。只有可靠计划和真实完成记录才判定按时/延期完成；未知历史用待核实，不能用阶段序号伪造完成事实。
5. 未完成阶段按最新计划比较上海当日；计划13日结束到14日才逾期。完成操作保留当时计划及完成时间，15日完成则红色延期2天；后续改其他计划不能改变既成结果，改期历史保留。重新打开/管理员纠正按最新有效阶段历史识别当前轮次，不误用旧完成结果。
6. 甘特图原位双排：上灰整体计划、下排工程师阶段计划，宽度按计划天数，不是百分比或实际耗时；前两环节无排期不强造段。旧项目无排期保留整体条并标待排期。跨月裁剪、同日交接、短段、无数据和长名称需可读。
7. 甘特颜色与卡片同源。保留230px固定左列、42px固定表头、日期最小36px与双向滚动，参照05预览增周末浅底与今日虚线。左列含主负责人、协作人员和当前环节，长名单适度折叠，提示展示全名单。提示跟随鼠标，按实际提示框宽高自动边缘避让，滚动/切月/筛选/离开关闭，不遮挡指针；支持键盘查看与原详情入口。
8. 测试须覆盖并发编号、回填、角色例外保持和撤销、上海日期边界、延期完成冻结、无计划历史、纠正/重开、筛选联动及甘特跨月/短段/鼠标边缘/滚动。保留旧流程、权限、审计与附件。

## 原位映射与完成标准

前两环节依照01预览：有真实受理/立项完成记录时显示绿色“已完成”，不宣称“按时”；没有真实历史则仍为待核实。工程师排期后的当前环节若尚未开始，保持灰色，超过计划结束日则红色。

- 日期：project-overview/index.vue、my-demands/index.vue；复用ElDatePicker，只补共用shortcuts。
- 编号：Prisma Project、创建事务与read-model；前端原卡片/详情/待办/清单/搜索，内部路由不变。
- 人员：现有钉钉同步和身份解析、分派校验及project-fields；复用现有用户和审计，不新增假用户。
- 阶段：stage-progress、stage-history、阶段完成记录；共享真实状态计算。
- 甘特：monthly-gantt组件与既有gantt-service；不复制预览的写死日期/身份，不另建页面。
- 完成以类型检查、单测、隔离数据库集成、生产构建及真实浏览器验收、独立代码审查为准。
