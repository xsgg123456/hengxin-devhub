# 服务器联调记录 · 2026-09-09

目标：`192.168.1.245`，新系统 `/opt/it-project-console`。用户授权独立部署、复用旧配置；管理名单最初四人，现按最新要求仅保留林炳辰（Albert）、姚泽攀。企业验收通过前保留旧公开入口。

## 最新管理名单调整（2026-09-09）

迁移后真实浏览器只读验收补充：用户用吴永杰账号完成钉钉扫码，workspace返回IT部/engineer，迁入74=18进行中+56完成、74份迁移说明，含原联调总75。全部项目图表、事业三部工作台详情中的占位负责人/补值说明已查看；本人范围加载后11张卡片均含吴永杰。2026-09甘特显示58条月份范围内项目，时间条/百分比/风险文字已目视核验。截图 `output/server-browser/migration-engineer-overview.png`、`migration-engineer-gantt.png`。本次未提交任何业务修改；页面最终留在全部项目。此前未完成的浏览器验收已补充上述只读范围，不代表同事账号写入流程已验收。

最新补充：后续已完成旧ACTIVE18/COMPLETED56共74项目导入，总项目75（含原测试1），管理员仍两人、通知队列仍6条且投递关闭。详见MIGRATION-ASSESSMENT实际执行结果；本页早期业务库存为历史时点。数据库恢复演练通过，对象存储恢复及真实浏览器迁入后视觉验收尚未完成。

- 已执行既有 ManagerGrantService：周熹、蒋燕 grant.active=false，角色均恢复 BUSINESS；部门分别保留事业三部、经营管理中心。林炳辰、姚泽攀均为 MANAGER，active grant总数为2，初始化管理员仍为姚泽攀。
- 执行证据：`docker exec -i -e APPLY_ROSTER=true itpc-prod-api-1 node --input-type=module`（输入本地运维脚本 `output/manager-roster-two.mjs`）返回 `applied:true, activeManagers:2`，四人管理鉴权结果分别与保留/撤销名单一致，REMOTE_EXIT=0；后续只读重查一致。
- 脚本语法检查通过，两阶段有界审查PASS。无应用源代码变更，无重新构建部署；本次验证为持久化名单及服务鉴权，未冒用四人扫码登录。
- 变更前备份 `/opt/it-project-console/backups/20260909-before-two-manager-roster.dump`，71,799字节、0600；备份文件已生成不等于恢复演练通过。服务保存撤权审计，另附实际用户授权运维来源。
- 下文四人配置及验收描述是当时记录，以本节最新名单为准。旧系统资料尚未导入；新库业务库存仍为1个项目、1条需求、2个附件。

## 依赖与构建准备

- Vite 7.1.5 → 7.3.6；SheetJS 0.18.5 → 官方 0.20.3 tarball，锁文件固定 SHA-512。原 Art Excel 组件未改。
- 类型检查、185 个单元测试（Web 130/API 55）、生产构建、锁文件重装通过。
- `pnpm audit --prod --json`：所有等级均为 0。原始记录在 `output/dependency-remediation-*`。
- 发布专项 5 项通过，包括真实 Nginx 成功、OAuth/S3 502、健康失败、静态资源 404 日志不泄露查询串或 Referer；访问日志使用 server 层安全格式，覆盖基础镜像默认 combined 日志，server 层统一关闭未经脱敏的错误日志。
- [SheetJS 官方发行方式](https://docs.sheetjs.com/docs/getting-started/installation/nodejs/)，[Vite 安全修复](https://github.com/vitejs/vite/security/advisories/GHSA-fx2h-pf6j-xcff)。依赖审计不替代基础镜像与运行环境安全验收。

## 服务器初始状态

只读确认：CentOS 7 / kernel 3.10.0-1160.el7，Docker 24.0.9、Compose 2.27.1，磁盘可用约 133 GiB。旧应用/代理/数据库/MinIO 均正常；新目录尚不存在，18080 未占用。

## 执行与验收

| 项目 | 状态 |
|---|---|
| 安全依赖修复 | 通过 |
| 新版发布包与校验 | 通过：当前 20260909-bd92912-uat；上传校验、产物隐私和部署健康通过 |
| 服务器独立安装和健康 | 通过：六个数据库迁移完成，API/Web/PostgreSQL/MinIO/独立 HTTPS 代理均 healthy；旧服务继续运行 |
| 配置沿用与钉钉组织同步 | 通过：181 名有效成员、54 个部门；server.env 权限 0600，演示登录与消息发送关闭 |
| 四位管理员配置 | 通过：姚泽攀为首位管理员，周熹、蒋燕、Albert 均已授权；Albert 的当前钉钉姓名为林炳辰，Union ID 已与旧库核对一致 |
| HTTPS 预览、附件及权限检查 | 通过本轮范围：真实文件表单上传/下载完整性、签名拒绝、工程师审批拦截及临时管理授权/撤销；细项见下方 |
| 用户真实钉钉授权登录 | 通过：张帅真实会话返回 IT部 / ENGINEER，页面显示 IT工程师；未伪造身份 |
| 公开入口切换 | 企业验收前不执行 |

预览采用只读旧 TLS 卷与独立回环代理，配合 SSH 隧道和专用浏览器域名解析；不改变旧代理配置、系统 hosts 或其他浏览器的访问结果。不使用演示登录或伪造 OAuth 成功作为企业验收。

首次安装版本为 `20260909-f9ec611-uat`，当前 `current` 已升级至 `20260909-bd92912-uat`。首次迁移前数据库备份已生成；通讯录及管理员初始化后另生成 `/opt/it-project-console/backups/20260909-post-directory.sql`（90,246 字节，权限 0600），恢复验证仍待执行。初始化首位管理员 ID 曾多录一位，已按旧生产库核实修正并重建新 API 容器，通讯录同步后完成四人授权和审计。

独立浏览器已打开钉钉官方二维码页面，回调使用 `https://pmg.qhhengxin.top:8443/api/auth/callback/dingtalk`。真实扫码、角色和本轮核心业务操作已验证；公开入口仍指向旧系统。新库现保留 1 条明确标记的联调需求及关联项目，未迁移旧业务数据。基础镜像安全验收、MinIO 备份恢复、真实消息验收及切换前最终检查仍待完成。

修复前首次真实登录曾返回张帅、IT部、BUSINESS；角色修复后的实际结果见下方18:05验收记录。管理员候选接口 403，公开只读管理名单为 200，符合全员只读规则；IT部 应映射工程师，已修正源代码并通过 Web 136 项及 API 72 项隔离集成测试，已审查并部署。管理功能通过本人明确授权的临时管理身份验收，四位领导本人逐一登录不在本轮范围。

### 角色修复部署结果（17:18）

- `bd92912` 已经两阶段独立审查通过；完整提交门禁通过。首次门禁的原有健康检查用例 5.35 秒超时，原样重跑通过，未放宽门槛。
- 统一脚本生成并部署 `20260909-bd92912-uat`；依赖 high/critical 0、产物隐私审计 162 文件通过；更新前备份 `20260909T091734Z-109402.sql`，无待执行数据库迁移，新服务 healthy。
- 真实通讯录同步后，服务器查询张帅为 `IT部 / ENGINEER / active`；有效管理员仍为 4，两个 IT 部门有效用户误分 BUSINESS 数量为 0。
- 浏览器刷新后 /api/me 返回 401。检查确认浏览器未持有 `itpc_session` Cookie，服务端仍有 1 条未过期会话（有效期 8 小时）；未伪造或补写凭据，当时已重新打开钉钉授权页；后续真实登录及工程师页面验证已完成，见18:05记录。

### 真实业务闭环验收（18:05）

用户明确授权临时提升自己的管理权限以验收，结束后撤销。使用真实钉钉会话，通过页面提交 `【联调测试】IT项目管理系统验收-20260909`；需求 ID `6086c9c6-cf9d-4f26-8b53-e7888a0dec57`，关联项目 ID `cmttxi4zv004r07r1ui8u7b81`。

| 验收点 | 实际证据 |
|---|---|
| 真实工程师登录 | /api/me 200，张帅 / IT部 / ENGINEER，页面显示 IT工程师 |
| 需求材料提交 | 页面上传 PDF 763 字节、HTML 255 字节，均 READY；需求进入 PENDING，姓名和部门自动带出 |
| 附件下载 | 两份签名下载 200；SHA-256 分别为 c38428619a86058bba332da4e30bd621ca53683b9dc2caa4ca0260b2ff9742b9、3176637074abd58e6ff8052d0ff3727b88be23ebca432aec38d1428d11ceb302，与本机文件一致 |
| 工程师越权拦截 | 临时授权前，工程师审批请求 403，需求保持待评估 |
| 临时管理授权 | 既有 ManagerGrantService 授权，页面显示管理人员和评估按钮；额外记录本人授权的临时权限审计 |
| 真实管理表单立项 | 唯一主负责人张帅，P1；方案设计9月16日、上线9月30日、交付10月9日；需求 established、项目方案设计0% |
| 权限恢复 | 审批后通过既有服务撤权，服务器和真实会话均 ENGINEER，临时 grant.active=false；原四位管理名单保留，候选管理接口403 |
| 工程师进度表单 | 本人主责项目保存方案设计、进行中、25%，后端 version=2 |
| 风险与并发 | 用真实会话接口模拟阻塞，风险立即出现；旧version更新409；解除阻塞200，最终version=4、25%、in-progress、risks=[] |
| 图表展示 | 需求图表显示张帅/IT部1条；9月甘特图1项目、张帅、方案设计、正常推进、25%跨月，已截图核验 |
| 收尾 | 临时授权/撤权审计均存在，activeManagers=4，通知仍关闭；保留明确标记的测试项目供查看，不混称真实业务数据 |

证据保存在 `output/server-browser/`，最终截图 `uat-gantt-final.png`。提交成功关闭表单时产生两次附件清理409，服务端拒绝删除已引用材料，实际文件下载完整；这是控制台噪音，本次不冒充无任何报错。自动化下拉等待因页面状态变化调整后完成，不绕过页面立项。没有验证普通非IT账号、协作者独立写权限、实际消息发送或备份恢复，本记录不代表全部上线验收通过。

## 钉钉真实定时通知单人验收（2026-09-09 19:02）

用户明确指定只发给张帅。独立恢复库复用部署中的 startDingTalkJobs 每60秒调度和 NotificationService 投递/回执逻辑，只为测试进程启用投递。消息标明联调测试、无真实分派；实际 tick1 在60秒 accepted=1，tick2 在120秒 sent=1/state=SENT/sendCalls=1。正向回执时间2026-09-09T11:02:13.166Z（北京时间19:02:13），任务ID3434172649884，无失败或重发。正式配置仍notificationsEnabled=false，生产队列仍6条；生产审计action=dingtalk_scheduled_test已保存。测试进程退出0，独立测试库itpc_notification_test_20260909已删除，查询剩余0。

有界两阶段审查通过，外层330秒硬超时保护。本次验证每分钟调度与单人真实送达，不代表用户已阅读；工作日09:00管理风险汇总未向领导实发，正式全量通知仍未启用。

## CSV20替换验收（2026-09-09 19:33）

当前业务数据以最终CSV20条为准，覆盖此前75项目/1测试需求的数量说明。已备份并事务替换为18 ACTIVE项目、2 PENDING需求、0附件元数据、0通知队列；旧测试附件物理对象保留备份。正式通知关闭，管理名单林炳辰、姚泽攀。生产20行名称、部门、开始/交付日期和原始审计逐项一致。

10/10映射测试、两阶段审查、隔离替换/幂等人工修改保护/20表全内容故障回滚通过。真实吴永杰会话workspace200，页面全部项目18张、需求池2待评估，需求部门分布各1条，9月甘特图9条相交项目。3个项目主负责人和1个需求提出人暂占位；阶段百分比及上线日期是已标记的迁移估值。详见MIGRATION-ASSESSMENT最后一节。没有开启全量通知或切换公开旧入口。

收尾连接恢复：浏览器切回项目页时SSH隧道断开，网络日志为ERR_CONNECTION_REFUSED；重新建立本机8443到服务器18443的隧道后刷新，全部18卡片恢复，networkError=0，重新保存csv20-projects.png。此过程未重跑数据导入。

## 正式上线收尾（2026-09-09）

备份及隔离恢复、每日备份、入口新旧候选验证已完成；实际镜像安全扫描发现CRITICAL，正式切换门禁拒绝且路由未改。最新完成项、证据、剩余阻断及修复路线见GO-LIVE-CHECK.md。此前应用依赖0漏洞不能解释为基础镜像也无漏洞。

## 正式部署切换（2026-09-10）

09:42正式入口已改为新系统，版本20260910-go-live-v2。用户要求功能及上线收尾优先，停止扩大底层整改；镜像原始结果、暴露面和限制保留，不能声称全部漏洞已修复。API/Web更新，PostgreSQL/MinIO/mc不变。

完整构建、136前端/55后端/72集成/6发布/7真实浏览器流程、有界审查通过；新完整备份和部署SQL备份完成。17表摘要完全相同，人员/部门/配置更新时间和一条审计来自钉钉通讯录正常同步。18项目/2需求、管理林炳辰和姚泽攀、正式通知关闭。

正式域名首页及钉钉入口已用普通浏览器打开；公开TLS代理的实际预签名附件上传/下载内容一致、匿名拦截和测试清理通过。按用户要求改用Chrome并完成真人扫码，身份张帅/IT部/ENGINEER，/api/me与workspace均200；本人3项目、全部18项目、需求2条、9月甘特9项目及切月、详情、取消进度编辑通过，管理专用接口403。项目和需求图表已查看截图。完整证据及已知限制见GO-LIVE-CHECK最新一节；本轮既定发布验收完成。
