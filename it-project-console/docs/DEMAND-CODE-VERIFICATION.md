# 需求公开编号验收（2026-09-11）

用户确认需求列表和详情改用 `XQ-YYYY-NNNN`，本次仅本地实现，未部署。

## 实现

- 数据库独立需求年度计数器和触发器，首次保存含草稿按创建时上海年份取号；并发唯一、超过四位自然扩展、删除不复用、编号不可编辑。
- 存量按创建时间/内部ID稳定补号，内部ID、项目关联及附件关联保持不变。需求读模型返回公开编号。
- 原需求列表和760px详情抽屉仅替换编号文本；提交/草稿保存/修改成功提示同步使用公开编号，缺号显示“编号待同步”。原型生成、存储迁移和计数器同步支持。

## 验证

- 前端 `pnpm --filter @it-project-console/web test`：31文件172项通过。
- 前端 `pnpm --filter @it-project-console/web build`：类型检查通过，3374模块，构建57.59秒，退出0。
- 后端 `pnpm --dir it-project-console/api test:integration`：15文件101项通过，26.79秒；独立schema和Bucket已回收。覆盖并发、跨年、草稿/提交/立项保号、删除不复用、四位扩展、旧数据回填和重复迁移。
- 原型浏览器首轮35通过、10失败（新增用例按钮名一次不符，其余页面加载/交互超时）；修正为原型已有“重新提交”按钮后，以 `playwright test --last-failed --workers=2` 复跑10/10通过，42.5秒。45个独立用例均有成功记录。
- 实际列表截图：`output/playwright/demand-code-list.png`，新需求显示XQ-2026-0005，存量显示0001～0004。主Agent检查截图，独立reviewer使用浏览器核对列表、详情及相邻项目页，编号一致且无截断。
- 独立code-reviewer：Stage 1及Stage 2通过，无HIGH/MEDIUM问题。
- 后端单测：17文件66项通过（1.93秒）；后端构建 `pnpm --dir it-project-console/api build` 通过，退出0。
- 真实API浏览器首轮7/7通过（1.4分钟），独立schema/Bucket已回收；截图额外发现提交提示仍带内部ID，已补齐公开编号。
- 成功提示补齐后，前端172单测再次通过（1.74秒），类型检查与构建再次通过（22.29秒，日志见根目录`output/demand-code-web-build.log`）；需求浏览器专项1/1通过（13.4秒），验证成功消息和列表详情编号一致。首次新增消息断言因同时存在上传提示而匹配多元素，定位到提交提示后复验通过。
- 成功提示补齐后的真实API浏览器复验：7/7通过（1.4分钟），退出0，独立schema/Bucket已回收；日志见根目录`output/demand-code-live-browser.log`。包含真实提交消息、列表/详情编号及立项后保号断言。
- 最终独立reviewer复审成功提示刷新时序、失败分支和整体改动，无HIGH/MEDIUM问题。

首轮并行运行构建、浏览器和集成造成超时；API单测同时遇到Prisma生成文件读取失败。以串行复跑结果为最终依据，不将失败和跳过计为通过。
