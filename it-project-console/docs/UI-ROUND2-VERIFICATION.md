# 第二轮优化验收

2026-09-11，本地实现，未部署。范围见 UI-ROUND2-CONFIRMATION.md 正式验收条款。

## 实现与证据

- 两处业务日期区间复用原 ElDatePicker，共享九项上海日期快捷筛选。工程师排期表单不加筛选快捷项。
- 项目公开编号由数据库年度计数器和触发器事务生成；存量按创建时间和ID稳定回填，内部ID不改。原型本地计数器持久化，删除不复用。
- 工程师例外资格采用用户唯一ID，贯通登录、同步、撤管理权、候选、分派和协作。周熹生产账号仅做只读核实；部署后按 ENGINEER-OVERRIDE.md 执行一次授权才生效，未恢复管理权限。
- 完成记录保存当时计划快照；卡片、详情、甘特共用阶段状态，旧未知历史不推断完成。当前未开始灰色，延期完成红色且保留延期天数。
- 原甘特组件保留固定左列、表头和双向滚动；下排按真实阶段计划长度显示，同日重叠分层，周末背景、今日虚线及鼠标跟随浮层支持边缘避让、滚动关闭、键盘与点击详情。

## 验证记录

- Web 类型检查通过；前端单测168项（含新增未开始灰色回归），API单测66项。
- 隔离API集成14文件99项通过：重复迁移、编号并发/跨年/回填/不复用、计划冻结/纠正、人员资格保持/撤销/实际分派。独立测试schema及Bucket回收。
- 浏览器共44个独立用例已有成功结果：首轮43项41通过，两个旧整行按钮断言随新组件更新；对应13项复验12通过，剩余悬浮用例补充滚动后的真实鼠标移动后与新增阶段用例2/2通过。新增用例又单独1/1通过取视觉证据。
- 新增阶段浏览器用例直接验证计划13日/完成15日显示延期2天、改计划不改既成结果、五分段同日分层、指针定位、边缘避让、滚动关闭、键盘聚焦/Escape及详情。
- Web生产构建通过（Vite 3373模块，45.53秒）；API类型和构建通过。最终提交门禁再次执行前后端类型/单测/构建、发布测试及隔离集成。
- 实际截图（本地测试数据，不是生产记录）：it-project-console/output/playwright/round2-card-actual.png、round2-gantt-actual.png。已人工检查卡片状态与双排甘特、浮层可读性。

独立 code-reviewer 两阶段通过，无遗留 HIGH/MEDIUM；审查发现的已排期未开始状态误蓝已修复并独立7/7复验。补充测试与文档复核通过。

真实API浏览器复跑7/7通过（1.3分钟），独立schema/Bucket回收。首轮6/7通过，唯一失败为自动滚动关闭浮层后未再次移动鼠标；补真实鼠标移动及“整体计划”正向断言后通过。日志在根output/round2-live-browser.log。
