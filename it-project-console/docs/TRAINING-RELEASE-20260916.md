# 2026-09-16 教程素材发布记录

用户已提供素材并授权生产部署。保留资源卡、播放器、ZIP获取流程；只在既有页面传入实际资源，不执行ZIP内任何脚本或说明。

## 交付素材

路径前缀：`/training/20260916/`。随Web镜像发布，同源静态访问；缺失文件返回404，不回退首页。版本化路径长期缓存，替换内容必须使用新路径。

| 文件 | 字节数 | SHA256 |
| --- | ---: | --- |
| business-prd-prototype-tutorial.mp4 | 68581041 | b87844c03d03ae30d216380b25043ae4e557ddfc9883a38f7c89ee5899ac54df |
| business-prd-prototype.zip | 30486 | bae39d30ddcc2b3d2d34385ce914668ba1f0fa9489ad8e58de5693ebc7ed5a29 |

ZIP只读扫描：目录无路径穿越/私钥/数据库/环境文件，无开发者路径或常见密钥模式；不安装该Skill。视频按原文件交付，不改变内容。

## 发布前验证

- 浏览器素材播放、下载原字节、关闭释放、网络失败重试和原提交/窄屏回归：4项通过。
- 生产依赖审计：critical/high/moderate/low均为0。
- 前后端类型检查通过；真实Nginx素材哈希、MIME、206分段响应及404共2项通过。独立审查Stage 1与Stage 2通过，无阻塞项。
- 发布包完整检查通过：前端323/后端112单测、169隔离集成、11发布测试；62项浏览器回归全部通过。Docker生产构建通过，200份文本产物隐私扫描通过。

## 边界

- 资源区业务自愿使用，无学习记录，不改变提交资格。
- 本次不删除待审批项目，不校正历史名称记录，不改生产凭据，不开放演示登录。
- 前一生产版本：20260915-f8660fc-r1；部署前数据库备份，保留上一版本供兼容回退。
- 上线状态：2026-09-16已发布，版本 `20260916-def6f3c`，源码提交 `def6f3c`，构建清单dirty=false。

## 生产验收

- 正式入口：`https://pmg.qhhengxin.top:8443`。首页与`/health/ready`返回200；API、Web、PostgreSQL、MinIO均healthy。
- 用户提供的两个文件经正式HTTPS入口完整下载后，字节数与SHA256均匹配上述清单。
- MP4分段请求返回206、`Content-Range: bytes 0-1023/68581041`、`Content-Type: video/mp4`；缺失资源404。
- 真实浏览器通过生产视频地址解码并播放：1920×1080，时长601.327秒，readyState=4，播放时间推进至14.56秒，无媒体错误。
- 未登录业务接口仍返回401，生产登录页正常；未绕过钉钉登录，也未在生产创建测试需求。
- 数据库备份：`/opt/it-project-console/backups/20260916T041407Z-57970.sql`。迁移检查显示无待执行项；迁移哈希与上一版本相同。
- current=`20260916-def6f3c`；previous=`20260915-f8660fc-r1`；无pending发布。
- 按既有保留策略移除服务器及本机的过期发布包`20260915-9c373e6`；保留当前与上一成功版本。业务数据、数据库备份未删除。被移除旧包可从对应源码重新构建，不属于直接恢复保证。
