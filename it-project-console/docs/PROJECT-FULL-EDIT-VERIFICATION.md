# 正式项目完整编辑整改验收

## 交付范围
- 指定审批管理员长期编辑所有项目业务资料，迁移项目当前主负责工程师临时编辑；真实数据库状态控制核实和转交后的权限。
- 独立需求首次提出日期；录入/展示/项目与需求同步；内部系统时间不替代业务日期。
- 当前人员归属、负载与待办跟随修改；验收改派旧待办通知失效并重新排队，原已发送历史不改。
- 独立需求提交、退回、重提、拒绝、撤回审计；完整编辑/核实留原因、作者及前后快照。
- 原前端卡片/详情/抽屉接正式API；保留预览模式与失败输入、未保存提示。

## 数据与接口
新增迁移：api/prisma/migrations/202609150001_project_full_edit/migration.sql。新增Demand/Project.firstRequestedOn、Project.businessOwnerId/description/migrationVerified。旧标记只在迁移时识别，不编造首次日期；有关联需求的旧项目继承业务负责人。显式清空后不再回退原提出人。
POST /api/projects/:id/edit 使用严格字段白名单、实时用户/项目权限、事务行锁、版本校验及幂等回执。验收结果、项目编号、实际历史时间不可借编辑接口伪造。已关闭项目可纠正资料，阶段修改仍须重开。
七阶段计划存储与读取兼容原日常排期入口；预计上线/交付与相应计划结束保持一致。月度统计算法保留。

## 已完成验证
- API单测：pnpm --dir it-project-console/api test，21文件106用例通过。
- API类型/构建：node node_modules/typescript/bin/tsc --noEmit；pnpm --dir it-project-console/api build，均退出0。
- Web单测：43文件248用例通过；pnpm --dir it-project-console/web build（含vue-tsc），3388模块、26.71s、退出0。
- pnpm --dir it-project-console/api test:integration：20文件136用例 + 3文件13用例（149）；15次迁移及重复deploy通过，schema及桶回收。首轮新增用例创建身份影响固定种子数量断言，已调整到种子检查后执行，复跑全过。
- code-reviewer两阶段审查通过；审查发现清空业务负责人回退问题已修复并新增真实验收回归。

## 边界
本轮未部署、未修改生产数据、未发送真实通知。原需求提出人与历史操作人不改；既有删除权限保持，不把完整编辑扩展为新增删除授权。首次日期为空的旧项目须人工核实。
生产发布仍需用户明确授权，按原备份/迁移/回退流程执行，不能把prototype产物用于生产。

## 浏览器与工程门禁进展
- 正式完整编辑管理员持久化/需求同步与迁移主责核实回收：2/2通过，23.3s；首轮测试样例不存在，已新增仅isolated test schema可运行的fixture，不增加生产入口。
- 正式业务验收1/1、排期协作/重开1/1、看板/管理员名单2/2通过。看板首轮冷加载空白15s超时，单独复测51s完整业务流程通过，未放宽等待阈值。
- 原型全量50项：49通过、1项业务三档截图达到30s上限；定向复测该项28.0s通过（进程退出0），因此全部50项均有通过记录，不声称首轮全绿。
- Harness 20/20、发布配置9/9通过；首次受Python路径/Docker沙箱限制，使用可用Python PATH和获准的本机Docker测试权限后复跑通过，未绕过门禁。
- 收尾修复：无结构化计划的旧项目保留原stageExpectedDate；修改基础资料不再隐式清空阶段截止日期。显式移除旧计划或改变阶段仍按对应计划重算。Web248单测再次通过。
- 正式登录3/3、方案验收1/1、工作流2/2通过；全部7组真实浏览器共12项通过，每组独立schema与桶均回收。
- 旧阶段日期兼容增量独立审查通过。审查者环境未能运行测试，动态证据采用主Agent的Web248单测及提交门禁API隔离回归；显式移除/换阶段清空分支已静态核对，未新增定向断言。
