# 交付后优化 · 前端交互预览

> 历史预览记录。用户已确认正式开发，当前规则与实现以 [OPTIMIZATION-ITERATION.md](OPTIMIZATION-ITERATION.md) 为准，下文的未确认/仅预览限制不再适用于正式实现。

2026-09-15：用户只授权基于现有前端预览方案，业务规则尚待确认。本轮不接服务端、不发布。

## 预览范围与验收

1. 原位复用 project-card.vue：已完成项目增加入口及关联数量，保留原字段、七环节、详情和编辑。
2. 原位复用 project-detail-drawer.vue：增加后续优化区域。新增 optimization-preview.vue 只承担新增优化内容；原需求编辑器强制附件并提交既有需求事务，不能直接承接未确认的轻量表单，因此新增局部预览组件，复用 ElDrawer/ElForm/ElDescriptions。
3. 预览通过 prototype 构建且 URL 含 optimizationPreview=1 才显示。演示视角切换明确标注，业务提交、姚泽攀审批、工程师接单、提交验收、验收通过可连续操作；退回与拒绝需说明，草稿可删除。共享内存记录，刷新重置，原项目不改动。模拟优化立项记录只在此区域展示，不声称接入正式项目、负载、通知或权限。
4. 附件本轮只显示选中文件名，可移除，不上传；表单必填标题、问题、期望结果和日期。关闭有内容表单需确认放弃。
5. 实际浏览器验证并截图：卡片入口、提交表单、审批、关联记录。类型检查和构建通过，独立审查预览边界。

## 执行顺序

源规则记录 → 原组件增加预览入口及局部组件 → 实际浏览器操作/截图 → 类型与构建/独立审查 → 展示预览。

七环节是否简化、删除主项目的关联规则、生产权限和统计均待后续需求确认，本轮不修改。

演示样例：在既有 seed.ts 内为本预览提供一个已完成项目（名称取用户截图，人员沿用演示账号），prototype-repository.ts 使用独立预览存储键，避免修改已有原型数据。

## 本轮验证记录
- web目录执行 node node_modules/vue-tsc/bin/vue-tsc.js --noEmit：exit 0，无错误。
- node node_modules/vite/bin/vite.js build --mode prototype：exit 0，3413 modules transformed，21.53s。
- 实际浏览器通过空表单拒绝、草稿编辑提交、审批改期、无原因退回拒绝、退回重开日期保留、接单、提交验收、业务通过；原项目仍已完成。
- 截图目录 output/playwright/optimization-preview。使用现有原型人员与历史阶段缺失样例，七环节待核实不伪造历史。
- 独立审查两项问题已修正：精确查询开关、审批日期独立存取。
- 当前仅交互预览；未执行正式API/生产测试，未发布。
