# 变更记录

## 2026-09-09 · 统一发布工具

- 新增 `pnpm release`，统一打包、上传、首次配置、更新、状态和受限回滚。
- 沿用旧项目的离线 Docker 发布流程，为新前后端增加生产镜像与独立 Compose 栈。
- 旧配置在服务器内提取，发布包不携带密钥；新数据库与文件桶独立，生产关闭演示登录。
- 部署前备份数据库，健康通过后更新成功版本；阻止跨数据库结构回滚和隐式基础设施升级。
- 本次交付发布工具与本地验证，不代表已切换旧生产域名。
